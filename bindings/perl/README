
Experimental perl bindings.

Incomplete. It seems like the perl module is being created correctly
and so is the wrapper library, however, the module is not installed
and the library is installed in the wrong place. Alas ...

Will take some autoconf fiddling to fix correctly.
